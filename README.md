# SnapNfit Landing (Next.js + Tailwind)

One-page marketing site. Images are placeholders—drop real assets into `/public` and replace the placeholder DIVs with `<Image src="/hero.jpg" .../>` etc.

## Quick start
```bash
npm install
npm run dev
